# Query - Me sirve solo para realizar consultas
scalar JSON

type Query{
    findyRoleById(rolId: ID!) : ResponseGraphQl
    roles: ResponseGraphQl
}

type ResponseGraphQl{
    code: String,
    message: String
    data: JSON
}
